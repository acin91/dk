<td>{{ object.id }}</td>
<td>{{ object.name }}</td>
<td>{{ object.createdAt|date('Y-m-d H:i:s') }}</td>
<td>
    <a class="btn btn-primary" href="{{ path('dk_admin_category_edit', {'id': object.id}) }}">
        <span class="glyphicon glyphicon-pencil"></span>
    </a>
    <a class="btn btn-danger" href="{{ path('dk_admin_category_delete', {'id': object.id}) }}">
        <span class="glyphicon glyphicon-trash"></span>
    </a>
</td>